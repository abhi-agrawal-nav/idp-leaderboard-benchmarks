# Model prediction caches (per-model, auto-created on first run)
caches/*/
!caches/.gitkeep

ground_truth/
results/
__pycache__/
*.pyc
.env
.venv/

# reproduction harness (additive)
.venv-serve/
*.log
.pytest_cache/
predictions/
normalized/
# internal design notes — not part of the shareable delta
CONTEXT.md
# local-only test scaffolding (kept on disk, not part of the shareable delta)
tests/
conftest.py
